[config]
; Targets Python 3.14 (SweepMe! 1.6.1+); vendors festo-edcon 0.14.9, the newest
; release compatible with the pymodbus 3.13 that SweepMe! 1.6.1 ships (the 1.0.x
; line pins an older pymodbus). Earlier SweepMe! versions use the previous release.
architecture = "3.14-64"
