[config]
; This driver version loads sibling modules via `from pysweepme import load_source`,
; which requires pysweepme >= 1.6.1 (Python >= 3.10, SweepMe! 1.6.1+).
; Earlier SweepMe! versions use the previous driver release.
architecture = "3.14-64"
